# Автоматическая обработка корпоративной почты

Проект читает письма из папки, переводит их в JSON, классифицирует по правилам и раскладывает исходные файлы по папкам категорий.
## Категории

- `incidents` аварии, сбои, критические инциденты, недоступность сервисов.
- `service_requests` запросы на доступы, VPN, почту, ПО и корпоративные сервисы.
- `spam` реклама, фишинг, подозрительные ссылки, просьбы ввести пароль 
- `hardware_faults`проблемы с техникой: принтеры, ноутбуки, мониторы, мыши, зарядки и другое оборудование.
- `прочее` -письма, которые не набрали достаточный балл 

## Как работает пайплайн

1 `app/app.py` берет папку с письмами в формате `txt`

2 `MailParser` выделяет тему, отправителя, получателя, дату, текст, ссылки и вложения.

3 Результат в `all_mails.json`

4 `classifier/main.py` загружает `all_mails.json` и таблицу весов `classifier/config.csv`.

5 Для темы и текста ищутся маркеры из таблицы.

6 Совпадения в теме получают больший вес, потому что тема обычно короче и важнее.

7 Побеждает категория с достаточным баллом и достаточным отрывом от второго места.

8 Если балл слишком маленький или отрыв неубедительный, письмо попадает в `прочее`

исходные файлы перемещаются в папки категорий



## ML классификатор

Помимо основной классификации по правилам в проекте есть ML классификатор.
Он находится в `app/ml_classifier.py` и использует модель из папки `models`. 




## Графический просмотр результатов

В проекте есть графический интерфейс для просмотра результатов обработки писем. Он реализован на `tkinter` в файле `result_viewer.py`.

Просмотрщик не классифицирует письма заново, а открывает уже готовый `result.json`, который создается после запуска пайпа.




## Быстрый запуск

```bash
./run_classifier.sh mails sorted
```

После запуска в `sorted` появятся:

* `all_mails.json`
* `result.json`


## Запуск по шагам

```bash
python app/app.py mails sorted/all_mails.json
python classifier/main.py sorted/all_mails.json mails sorted sorted/result.json
```

## Проверка

```bash
python -m pytest -q
python -m compileall -q app classifier tests
```

## Авторы

Хурматуллин Тимур, Дмитриченко Игорь, Королев Михаил, Унгунов Валерий, Дедешко Анна
